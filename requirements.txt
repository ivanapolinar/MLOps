-e .

# pinned runtime / serving
fastapi
uvicorn[standard]
pydantic
python-dotenv>=0.5.1
requests

# core data + modeling
pandas
numpy
scikit-learn
joblib

# ML lifecycle / experiment tracking (optional at runtime, required for some features)
mlflow

# CLI / utils / test / lint
click~=8.1.8
pytest
flake8
coverage
Sphinx
awscli

# visualization
matplotlib
seaborn

# local packaging helper
setuptools